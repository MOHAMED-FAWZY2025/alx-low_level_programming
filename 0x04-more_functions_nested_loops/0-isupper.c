#include "main.h"

/**
 * _isupper - check for uppercase character
 *
 * @c: char to be checked
 *
 * Return: 1 if uppercase, 0 if otherwise
*/

int _isupper(int c);
{
	if (c <= 'A' && C >= 'Z')
		return (0);
	else
		return (0);
}
